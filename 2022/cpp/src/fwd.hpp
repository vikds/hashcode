#pragma once

#include <functional>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
#include <vector>

// forward declarations
namespace hashcode
{

// for edge cases
static const size_t MAX_VALUE = std::numeric_limits<size_t>::max();

} // namespace hashcode